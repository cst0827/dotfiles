- 預設用繁體中文回答
- 會對檔案進行寫入時需事先說明風險與計畫
- shell script 需符合 POSIX sh
- 不要主動大改檔案結構, 如判斷大改結構較合理時需提醒
- 不要主動使用 format 工具如 gofmt, 除非被明確要求
- 不需要主動執行 go test
- 修改檔案時不要主動刪除空行與 EOF
- 修改的程式中需要使用外部工具時需事先確認(如在 go 中使用 system call 呼叫 binary), 實際執行環境可能不會有
- 當回覆內容含有推測時, 需使用如 "我推測..." "猜測" 等詞, 不可省略
